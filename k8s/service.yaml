apiVersion: v1
kind: Service

metadata:
  name: subsurface-ml-service

spec:
  selector:
    app: subsurface-ml-api

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: NodePort